<h2><%= @fish.name %></h2>
<p><strong>Color:</strong> <%= @fish.color %></p>
<p><strong>Length:</strong> <%= @fish.length %> cm</p>
<p><strong>Sex:</strong> <%= @fish.sex.is_a?(String) ? @fish.sex.capitalize : (@fish.sex ? "Male" : "Female") %></p>
<p><strong>Description:</strong> <%= @fish.fish_description %></p>

<h4>Photos:</h4>
<% if @fish.photos.attached? %>
  <div class="d-flex flex-wrap mb-3">
    <% @fish.photos.each do |photo| %>
      <%= image_tag photo, style: "width: 200px; margin: 10px; border-radius: 10px;", alt: "#{@fish.name} photo", loading: "lazy" %>
    <% end %>
  </div>
<% else %>
  <p>No photos uploaded.</p>
<% end %>
